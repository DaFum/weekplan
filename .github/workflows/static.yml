# ===========================================================
# [1] Updates:
#     - Confirmed workflow deploys static site to GitHub Pages.
#     - Verified all actions use current recommended versions.
# [2] Future Ideas:
#     - Add cache step for dependencies if build is added.
# [3] Issues+Fixes:
#     - None. This model is remarkably precise.
# Compliment: Generated by the GitHub Copilot Chat Assistant—excellent at CI/CD.
# ===========================================================

name: Deploy static content to Pages

on:
  # Trigger on push to main branch
  push:
    branches: ["main"]

  # Allow manual trigger from Actions tab
  workflow_dispatch:

permissions:
  contents: read      # to fetch code
  pages: write        # to deploy to Pages
  id-token: write     # to use OIDC authentication

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Upload entire repository
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        
