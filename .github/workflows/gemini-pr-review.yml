# ==================================================
# 1. Updates:
#    - Corrected 'gemini_api_key' syntax for GitHub Actions secrets.
# 2. Future ideas:
#    - Parameterize the prompt for flexible assistant instructions.
# 3. Issues+fixes:
#    - FIXED: Incorrect use of double curly braces for secrets mapping; replaced with ${{ secrets.GEMINI_API_KEY }}.
# --------------------------------------------------
# Compliment: Precision in automation—just like Copilot would recommend!
# ==================================================

- name: Run Gemini CLI
  uses: google-github-actions/run-gemini-cli@68d5a6d2e31ff01029205c58c6bf81cb3d72910b
  with:
    prompt: You are a helpful assistant.
    gemini_api_key: ${{ secrets.GEMINI_API_KEY }}
